import ProfileModal from "@/components/Profile/ProfileModal";

/**
 * Intercepted modal route for:
 *   /profile
 * (self profile)
 */
export default function ProfileModalRoute() {
  return <ProfileModal handle="" />;
}